server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — 所有 /rei/* 路由指向 index.html
    location /rei {
        try_files $uri $uri/ /rei/index.html;
    }

    # Cache static assets
    location /rei/assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
